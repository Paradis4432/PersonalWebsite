//var myInterval = setInterval(function () {
//    console.log('it works' + new Date());
//},1000); 

$(() => {
    /*

    setTimeout(() => {
        $("#loadMainText").text("> Loading.")
    }, 200);
    setTimeout(() => {
        $("#loadMainText").text("> Loading..")
    }, 400);
    setTimeout(() => {
        $("#loadMainText").text("> Loading...")
    }, 600);
    setTimeout(() => {
        $("#loadMainText").text("> Loading....")
    }, 800);
    setTimeout(() => {
        $("#loadMainText").text("> Loading.....")
    }, 1000);

    setTimeout(() => {
        $("#loadMainText").toggle()
        $("#mainHome").toggle() 
        $("header").toggle()
    }, 1150);
    */

    $("#loadMainText").toggle()
    $("#mainHome").toggle() 
    $("header").toggle()
})